#include <iostream>

using namespace std;
#define size 5
int main()
{
    int arr [size] ;

    int sum = 0, min, max,  keyToSearch , longestDistance = 0 ;
    cout <<"Enter elements of array : " << endl;
    //input data
    for(int i = 0; i < size ; i++)
    {
        cout << "element " << i+1 << " : ";
        cin>> arr[i];
        // find sum
        sum += arr[i];
    }
    //div
    cout <<"========================" << endl;
    //
    for(int i = 0; i < size ; i++)
    {
        cout << "element " << i+1 << " : " << arr[i] << endl;
    }
    // find min, max
    min= arr[0];
    max= arr[0];
    for(int i = 1; i < size ; i++)
    {
        if (min > arr[i])
        {
            min = arr[i];
        }
        if (max < arr[i])
        {
            max = arr[i];
        }
    }

    // find the longest distance between two similar numbers
    for (int i = 0; i < size; i ++)
    {
        int temp = 0;

        for(int j = 0; j < size; j ++)
        {
            if(arr[i] == arr[j]){
                temp = j -i;
            
            }
            if ()

        }
    }

    cout <<"========================" << endl;
    cout << "max = " << max << "\nmin = " << min << "\nsum= "<< sum << endl;
    cout << "enter the key to search about : ";
    cin >> keyToSearch;

    for(int i = 0; i < size ; i++)
    {
        if (keyToSearch == arr[i])
        {
            cout << "first found at : " << i;
        }
    }



    return 0;
}
